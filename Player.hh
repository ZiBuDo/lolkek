#ifndef PLAYER
#define PLAYER

#include <string>

class player {
public:
    std::string username;
    int stat1, stat2, stat3;

    player();
    //player(std::string username, int stat1, int stat2, int stat3);

};

#endif
