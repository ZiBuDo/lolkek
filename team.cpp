#include <string>

#include "team.hh"
#include "player.hh"

team::team() {};
//team::team(player top, player jng, player mid, player adc, player sup) {};

/*
    this->top = top;
    this->jng = jng;
    this->mid = mid;
    this->adc = adc;
    this->sup = sup;
*/
